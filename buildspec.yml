version: 0.2

phases:
  install:
    commands:
      - yarn workspace web install
  pre_build:
    commands:
      - yarn workspace web test
  build:
    commands:
      - yarn workspace web build
artifacts:
  files:
    - index.js