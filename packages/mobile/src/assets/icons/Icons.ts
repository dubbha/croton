const iconFb = require('./fb.png');
const iconGoogle = require('./google.png');

// TODO: Make svg icons
export const ICONS: { [index: string]: any } = {
  iconFb: `${iconFb}`,
  iconGoogle: iconGoogle,
};
