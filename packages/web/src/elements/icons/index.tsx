import React from 'react';
import { FontAwesomeIcon as Icon } from '@fortawesome/react-fontawesome';
import {
  faBookmark,
  faLeaf,
  faPlus,
  faSeedling,
  faMapMarkerAlt
} from '@fortawesome/free-solid-svg-icons';

export const BookmarkIcon = () => <Icon icon={faBookmark} />;
export const LeafIcon = () => <Icon icon={faLeaf} />;
export const PlusIcon = () => <Icon icon={faPlus} />;
export const SeedlingIcon = () => <Icon icon={faSeedling} />;
export const MapMarkerIcon = () => <Icon icon={faMapMarkerAlt} />;
